name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  push:
    paths:
      - '**.js'
jobs:
   chaitu:
     runs-on: self-hosted
     steps:
     - run: echo "file changes to index.js"
      
