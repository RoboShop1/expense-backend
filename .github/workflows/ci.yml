name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  workflow_dispatch:
    inputs:
      name:
        type: choice
        description: Who to greet
        options: 
        - monalisa
        - cschleiden
      message:
        required: true
      use-emoji:
        type: boolean
        description: Include 🎉🤣 emojis
      environment:
        type: environment
env:
  name: "dev"
jobs:
  job1:
     runs-on: ubuntu-20.04
     steps:
       - name: step-1
         shell: bash
         run: echo "MY Project Secret  ${PROJECT_NAME}"
         env:
             PROJECT_NAME:  ${{ secrets.PROJECT_NAME }}       
    
     
